apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ printf "%s-depl" .Values.product.label }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.product.label }}
spec:
  replicas: {{ .Values.product.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.product.label }}
  template:
    metadata:
      labels:
        app: {{ .Values.product.label }}
    spec:
      serviceAccountName: {{ .Values.product.serviceAccount | default "product-sa" }}
      containers:
        - name: {{ printf "%s-container" .Values.product.label }}
          image: "{{ .Values.product.image.repository }}:{{ .Values.product.image.tag }}"
          imagePullPolicy: {{ .Values.product.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - containerPort: {{ .Values.product.containerPort }}
          env:
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.config.label }}
                  key: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
            - name: SPRING_DATA_MONGODB_URI
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secret.label }}
                  key: SPRING_DATA_MONGODB_URI_PRODUCT
            - name: EUREKA_INSTANCE_HOSTNAME
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.config.label }}
                  key: EUREKA_INSTANCE_HOSTNAME
          resources:
            requests:
              cpu: {{ .Values.product.resources.requests.cpu }}
              memory: {{ .Values.product.resources.requests.memory }}
            limits:
              cpu: {{ .Values.product.resources.limits.cpu }}
              memory: {{ .Values.product.resources.limits.memory }}